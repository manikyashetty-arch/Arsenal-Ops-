fastapi>=0.109.0
uvicorn[standard]>=0.27.0
gunicorn>=21.0.0
pydantic>=2.5.0
python-dotenv>=1.0.0
openai>=1.10.0
sqlalchemy>=2.0.25
psycopg2-binary>=2.9.9
alembic>=1.13.1
httpx>=0.26.0
numpy>=1.26.0
PyPDF2>=3.0.0
python-docx>=1.1.0
python-multipart>=0.0.9
openpyxl>=3.1.5
orjson>=3.9
python-jose[cryptography]>=3.3.0
passlib==1.7.4
bcrypt==3.2.2
google-auth>=2.26.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.0.0
cachetools>=5.3

# Lint / formatting — pinned because CI runs `ruff check` + `ruff format
# --check` and version drift would cause spurious failures.
ruff==0.15.12

# Testing — pinned for the same reason: the `Backend (pytest)` CI job installs
# from this file, so a floating pytest could fail CI on a release the author
# never ran locally.
pytest==8.3.4

# Static type checking — pinned like ruff/pytest because the `mypy backend/` CI
# step installs from this file; a floating mypy could surface new errors on a
# release the author never ran. The two `types-*` stub packages give real typing
# for the JWT (python-jose) and password-hashing (passlib) layers.
mypy==2.1.0
types-python-jose==3.5.0.20260408
types-passlib==1.7.7.20260211