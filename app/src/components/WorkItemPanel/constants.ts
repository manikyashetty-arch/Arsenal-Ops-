// Superseded by the shared lib modules. Re-exported so existing importers
// (WorkItemPanel.tsx, AddSubtaskModal.tsx, index.ts) keep working.
export { TYPE_CONFIG, STATUS_CONFIG, PRIORITY_COLOR } from '@/lib/workItemConfig';
export { CALENDAR_CLASS_NAMES } from '@/lib/calendarClassNames';
